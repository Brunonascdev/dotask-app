import {Dimensions} from 'react-native';

const {width: WIDTH, height: HEIGHT} = Dimensions.get('window');

export default {
  primaryColor: '#6C63FF',
  deviceWidth: WIDTH,
  deviceHeight: HEIGHT,
};
